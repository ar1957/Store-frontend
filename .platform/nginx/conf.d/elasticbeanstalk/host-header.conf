# Pass the original Host header as X-Forwarded-Host to Next.js
# This allows the Next.js middleware to resolve the correct tenant
# when running behind the AWS Elastic Beanstalk ALB/nginx proxy.
proxy_set_header X-Forwarded-Host $host;
